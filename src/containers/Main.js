import React from 'react';
import BigText from '../components/BigText';

const Main = () => {
    return (
        <div>
            <BigText>Main</BigText>
        </div>
    );
};

export default Main;